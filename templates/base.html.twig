<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
        <link href="{{ asset('css/main.css') }}" rel="stylesheet"/>
        {% endblock %}
    </head>
    <body>
        {% block body %}
        <nav class="nav">
            {% block nav %}
                <a href="{{ path('app_course_list') }}">Kursy</a>
                {% if is_granted('ROLE_NAUCZYCIEL') %}
                    
                {% endif %}
                {% if is_granted('ROLE_MENEDŻER') %}
                    
                {% endif %}
                {% if is_granted('ROLE_ADMINISTRATOR') %}
                    <a href="{{ path('app_user_list') }}">Użytkownicy</a>
                {% endif %}
            {% endblock %}
        </nav>
        <div id="content">
            {% block content %}
            {% endblock %}
        </div>
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
